using UnityEngine;

public class FireTest : MonoBehaviour
{
    /*void Update()
    {
        if (Input.GetKeyDown(KeyCode.F)) // Appuyez sur "F" pour allumer
        {
            Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition);
            if (Physics.Raycast(ray, out RaycastHit hit))
            {
                TreeFire treeFire = hit.collider.GetComponent<TreeFire>();
                if (treeFire != null)
                {
                    treeFire.Ignite();
                }
            }
        }

        if (Input.GetKeyDown(KeyCode.E)) // Appuyez sur "E" pour éteindre
        {
            Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition);
            if (Physics.Raycast(ray, out RaycastHit hit))
            {
                TreeFire treeFire = hit.collider.GetComponent<TreeFire>();
                if (treeFire != null)
                {
                    treeFire.Extinguish();
                }
            }
        }
    }*/
}
